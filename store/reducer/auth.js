

const initialState = {
    token: null,
    userId: null,
    // didTryAutoLogin: false
}

export default (State = initialState, action) => {
    switch (action.type) {
        case AUTHENTICATE:
            return {
                toke
            }
    }
}